import React from 'react'

const SearchBar = () => {
  return (
    <div className='searchBar'>
        <input type="text" placeholder='Search by name, email, role' className="inputBar"/>
    </div>
  )
}

export default SearchBar